"use client"

import { useEffect } from "react"

export default function LucideProvider() {
  useEffect(() => {
    // This is just a placeholder to ensure Lucide React is loaded
    // The actual icons are imported directly in the components that use them
  }, [])

  return null
}

